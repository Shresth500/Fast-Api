version: '3.9'

services:
  redis-queue:
    image: valkey/valkey
    ports:
      - "6379:6379"
    volumes:
      - valkey_data:/data

  vector-db:
    image: qdrant/qdrant
    ports:
      - "6333:6333"
    volumes:
      - qdrant_data:/qdrant/storage

volumes:
  valkey_data:
  qdrant_data: